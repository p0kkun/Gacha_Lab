version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd app
        - npm install
        - npm run db:generate
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: app/.next
    files:
      - '**/*'
  cache:
    paths:
      - app/node_modules/**/*

